# AGENTS.md — microbiome-best-practices（16S 系列）Codex 工作指令

> 这份文件是本项目对自动化 agent（Codex 等）的**唯一权威指令**。所有"怎么写、怎么跑、什么能做什么不能做"的规则都在这里。开工前完整读一遍，写每一章时回查相关小节。

---

## 0. 项目是什么

- **目标**：一套面向"懂生物、不懂生信"科研人的 16S 微生物组分析教程，从零带到能独立复现顶刊图并投稿。**重点在下游分析与发表级美图复现**，上游只求能跑通。
- **形态**：Quarto 书籍型网站（参照 sc-best-practices），同时切片发公众号。网页版是代码权威源，公众号正文重讲解、轻代码。
- **仓库名**：`microbiome-best-practices`
- **现有资产**（就在本目录）：
  - `microbiome-best-practices-16S-大纲.md` —— 55 篇篇目、每篇内容要点、技术方案。**这是内容蓝图，按它写。**
  - `样板-22-非约束排序-PCoA.qmd`、`样板-24-约束排序-CAP.qmd` —— **两篇已定稿的样板，是所有章节的模具。写新章节前先读这两篇，照它们的深度、结构、写法套。**

---

## 1. 铁律（违反即返工，逐条务必遵守）

### 1.1 每章代码从三件套读起
每章 R 代码的输入统一是扩增子通用三件套 **`otutab` / `taxonomy` / `metadata`**（任何上游流程都会导出的格式），需要时再在章内 `phyloseq()` 组装。**不要从 `ps.rds` 之类中间产物起步**——那会让章节隐性依赖别篇，破坏"独立成篇"。

### 1.2 单篇自足：看一篇就能从头复现到出图
读者只看这一篇就能"装依赖 → 取数据 → 跑分析 → 出图"，不需翻别篇。具体：
- 每篇开头放一个可折叠 `<details>` 的【准备工作】块，**内联** `install.packages` 安装命令 + 作图函数定义（`pal_pub` / `scale_color_pub()` / `scale_fill_pub()` / `theme_pub()` / `save_pub()`）。
- **不要用 `source("R/theme_pub.R")` 作为复现路径**（单篇读者没有那个文件）。折叠块里可注明"整仓库使用者可改为 source(...)"。共享的 `R/theme_pub.R` 仍要建，供网站构建/整仓库用户用，但推文正文自足。
- 复现必需的代码（如 taxonomy 单列→7 列拆分）直接内联，不写"代码见第 X 篇"。
- 跨篇引用（"详见第 X 篇""下一篇讲…"）只服务"想深入的读者"，绝不能是复现的必经路径。

### 1.3 用真实、可引用的数据，不用玩具数据
- 严禁用 `vegan::dune` 这类生态学玩具数据。
- 优先：能写明出处的真实微生物组数据（注明可下载的文献 + 代码 GitHub/补充材料），或随 R 包分发的真实数据集（如 `microeco` 内置 `otu_table_16S`/`sample_info_16S`/`taxonomy_table_16S`/`env_data_16S`，CRAN 装包即得，兼顾"真实"与"单篇自足可跑"）。
- 数据不便给下载来源时，**至少 `head()` / `[1:5,1:5]` 展示数据结构**，让读者看清每张表长什么样。
- 章末给"参考"小节，列数据/方法/R 包引用。

### 1.4 图内文字一律英文
轴标签、图例、分组名、注释全部英文，中文只留在正文讲解。分组名若在 metadata 里是中文，读入后先 `dplyr::recode` 成英文再作图。原因：①无头 Linux 服务器默认无中文字体，ggplot 渲染中文变豆腐块（□□□）且常不报错、只是图丑；②图内英文可直接进英文论文。

### 1.5 正文不写"元说明"
读者可见正文里**不要出现写给作者/维护者的话**。删除或改写这类句子：
- 解释教程架构的："本篇不依赖前面章节可独立跑通""这正是全系列坚持 XX 约定的意义""接口只学一次""（第 18 篇已建好）""（即本文）"。
- 评论别人怎么写代码的：**不要写"溯源：作者代码通常长这样"这种段落**——sc-best-practices 从不这样写，直接用真实数据教。
- 生产环境细节（"无头 Linux 服务器"）不进正文，改写成对读者的好处（"避免部分环境缺中文字体时乱码"）。
- 保留：教学性跨篇导航、前置需要什么环境/数据、方法为什么这么做、常见坑、Methods 怎么写、参考。
- **判断标准：凡是在"解释教程本身"而非"教怎么做分析"的句子，删掉或改写成读者视角。**

读者可见标题和公众号正文还必须遵守以下边界：

- 编号只用于文件名、目录、manifest 和内部审计；公开标题不用 `（NN/55）`、`第 NN 篇`，独立文章不显示 `55.1` 这类自动章节号。
- 不注入"审阅草稿""开放审阅""草稿箱继续查看""Draft PR""QA/run key"等维护状态。
- 不暴露 AppID/AppSecret/token、草稿或素材 ID、私有 PR、绝对本机路径、用户名、内部主机/VPN/代理、agent 指令和受保护样本信息。
- 仓库相对路径可以出现在真正可运行的代码中，但正文不能用"结果保存在 `data/...`"代替实际结果、表格和解释。

### 1.6 确定性
每个含随机过程的步骤（`rarefy_even_depth`、NMDS、`adonis2`、置换检验、随机森林……）必须 `set.seed`。否则重跑图就变，无法区分是改动生效还是随机波动。

---

## 2. 执行与出图（服务器实跑）

- **下游出图章节**：frontmatter 用 `execute: eval: true` + `freeze: auto`。真跑出图、结果缓存进 `_freeze/`，重渲染不重跑，只在数据/代码变化时重算。跑出的图直接嵌入网页和公众号。
- **上游安装/QC 章节**（QIIME2 安装、SILVA/GTDB 数据库、SEPP 建树、PICRUSt2）：又慢又吃资源、几乎不产出推文用图。**只跑一次抓日志和截图**，不纳入每次渲染（`eval: false` + 贴日志/截图）。
- **代码写成真能跑的完整版**：会被执行的位置不要留 `<GitHub URL>` 这类占位符（占位只能出现在不执行的 Methods 模板等处）。
- 图内 300+ dpi、矢量优先（`save_pub()` 已封装 PDF+PNG，物理尺寸按期刊分栏 mm）。

---

## 3. 每篇覆盖九项功能，但不公开展示固定模板

九项功能用于作者自检，不是要求读者反复看到相同小标题。公开标题必须描述本章实际问题，参照 sc-best-practices 的思路组织为"动机/问题 → 真实数据或研究 → 实际结果 → 方法选择与权衡 → 推荐与局限"。

1. **实际问题与目标证据**：直接说明本章要解决什么；只有确实围绕某张论文图复现时才讨论目标 Figure。
2. **原理与关键结论**：先给可执行推荐，长推导折叠。
3. **数据与环境**：技术章内联安装、数据和作图函数；阅读/设计章直接展示真实案例或材料，不列作者待办清单。
4. **完整分析或判读**：从三件套/真实数据起步；无统计代码的章节也必须展示实际证据表和判断结果。
5. **方法选择与局限**：比较当前方案、替代方案和敏感性，而不是使用"审计与升级"作为固定公开标题。
6. **结果呈现**：按主题说明图表、措辞和导出，概念章不强塞"出版级美化"。
7. **常见误区**：覆盖审稿人高频攻击点，通常用 `callout-caution`。
8. **报告规范**：给出与本章实际分析一致的 Methods 或 reporting 模板。
9. **迁移到读者问题**：说明输入、设计或参数如何替换；标题按具体任务命名。

建议在开头放 2–5 条真正能改变分析决策的 key takeaways。用 Quarto callout 和 `<details>` 实现"先结论后原理"，但不把维护流程、内部路径或教程架构写给读者。

---

## 4. 仓库结构（首个任务：搭出来）

```
microbiome-best-practices/
├── _quarto.yml            # 站点配置：55 篇章节树（按大纲编号顺序）、主题、导航、搜索、暗色模式、代码复制
├── index.qmd              # 首页 = 第 01 篇 图集预览
├── chapters/              # 每篇一个 .qmd，文件名带编号 + 英文 slug（如 22-ordination-unconstrained.qmd）
├── R/theme_pub.R          # 共享作图函数（内容同样板【准备工作】块，供整仓库用户 source）
├── env/
│   ├── qiime2.yml         # conda 环境，锁版本
│   └── renv.lock          # R 包锁版本
├── data/
│   ├── download.sh        # 原始数据（FASTQ 数 GB）不进 git，从 Zenodo/figshare 拉
│   └── small/             # 小数据（示例三件套、中间产物）可进 git
├── figures/               # 出图产物
├── Dockerfile             # 一键可复现环境
├── .github/workflows/publish.yml   # push 后自动 quarto render + 部署 GitHub Pages
├── LICENSE                # 内容 CC-BY-4.0，代码 MIT
└── README.md
```

- 两篇样板移进 `chapters/` 并按 slug 重命名（`样板-22-…` → `22-ordination-unconstrained.qmd`，`样板-24-…` → `24-ordination-constrained-cap.qmd`）。
- 文件名/URL 用英文/拼音，为将来英文版和检索留路。

---

## 5. 开工顺序（Codex 按此推进）

1. **搭脚手架**：按 §4 建目录、`_quarto.yml`（含 55 篇章节树，编号/标题取自大纲）、`R/theme_pub.R`、README、CI、LICENSE。把两篇样板移入 `chapters/`。
2. **锁环境**：装好 QIIME2 + R 包后生成 `env/qiime2.yml` 和 `env/renv.lock`、写 Dockerfile。这是"完整跑一遍不报错"的前提。
3. **逐章定死数据出处**：为每章选定真实数据（文献+代码链接，或包内置数据集），记录在该章开头和 README 的数据清单里。缺数据的章节先标记、不硬编造。
4. **渲染两篇样板验证**：`quarto render` 跑通 22、24 两篇，确认 `eval:true`+`freeze` 出图正确、图内全英文、无豆腐块，作为流水线验收。
5. **按大纲编号顺序写其余章节**：每篇照 §3 结构、守 §1 铁律、§2 执行规范。写完 `quarto render` 验证能跑出图再进下一篇。
6. 上游章节（07–16）按 §2 只跑一次抓日志/截图，不进每次渲染。

**特别注意——需额外数据的三章**（大纲已注明）：第 45 篇生存分析（要临床随访：生存时间＋事件）、第 52 篇 SEM（要实测环境因子＋表型）、第 54 篇孟德尔随机化（要外部 GWAS 汇总统计）。这三篇开头必须明确写"你还需要什么数据"；MR 用公开示例（TwoSampleMR 内置 MiBioGen 暴露 + 公开结局 GWAS）演示以守住单篇可跑。

---

## 6. 验收标准（每篇写完自检）

- [ ] 从三件套/真实数据读起，`quarto render` 能真跑出图（下游篇）
- [ ] 单篇自足：【准备工作】块内联依赖与作图函数，未用 `source()` 作复现路径
- [ ] 用真实可引用数据，有 `head()` 展示 + 章末参考
- [ ] 图内全英文、无豆腐块；PDF/SVG 矢量 + TIFF/PNG 300–600 ppi（不写"矢量图 dpi"）
- [ ] 开篇是与本章匹配的实际问题；只有确有必要才展示目标论文图
- [ ] 无"元说明"、无"作者代码通常长这样"段落
- [ ] 标题无篇数分数、正文无自动章节号和内部审阅/QA/私有链接
- [ ] 实际结果或证据已在正文展示，未用内部文件路径代替内容
- [ ] 随机步骤有 `set.seed`
- [ ] 九项教学功能齐全但标题按主题命名；常见坑和 Methods/reporting 模板与本章匹配
